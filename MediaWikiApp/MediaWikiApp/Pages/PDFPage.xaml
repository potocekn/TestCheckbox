<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:AppBase.Resources"
             x:Class="AppBase.PDFPage">
    <ContentPage.Content>
        <StackLayout BindableLayout.ItemsSource="{Binding Items}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Grid RowDefinitions="1*, Auto">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="100" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Row="0" Grid.Column="0" Text="{Binding ResourceName}" Command="{Binding OpenResource}" BackgroundColor="GhostWhite"/>
                            <Button Grid.Row="0" Grid.Column="1" WidthRequest="100" Text="{x:Static resources:AppResources.ShareButton_Text}"  Command="{Binding ShareResource}" BackgroundColor="GhostWhite"/>
                        </Grid>
                    </StackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
            <Button Text="Test resource" Command="{Binding OpenResource}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>